import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => {
  return (
    <Layout>
      <h1 className={`flex justify-center items-center bg-backBlack min-h-[calc(100vh-60px)] text-white text-[600px] font-bold m-0 p-0 mobile:text-8xl`}>404</h1>
    </Layout>
  )
}

export default NotFoundPage
